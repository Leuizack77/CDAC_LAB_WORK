//Declare Array with String Data 
//display every element in uppercase format
// (use prdefined methods of String)

let months=["jan", "feb", "march", "April", "may", "june", 
"july", "Sepetember", "october", "november", "December"];

let days=["sunday", "monday", "tuesday", "wednesday",
"thursday", "friday", "saturday"];

console.log(months.map((abc)=>abc.toUpperCase()));
///console.log(months.map((abc)=>abc.toLowerCase()));

console.log(days.map((abc)=>abc.toUpperCase()));
//console.log(days.map((abc)=>abc.toLowerCase()));